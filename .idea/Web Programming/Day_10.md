# Day 10

## Document Object Model (DOM)
- How we interact with the web page
- Hierarchy of the HTML elements

** We opened the browser, went into dev tools and used the console.dir(document) command to see the DOM.

## Selectors
- document.getElementById()
- document.getElementsByClassName()
- document.getElementsByTagName()
- document.querySelector()
  - first thing that matches our criteria
- document.querySelectorAll()
  - everything that matches our criteria
  
** Notice that DOM manipulation starts with the document object.

## Selecting Styles
- document.selector.style.property = value;
- We have to adjust how we call things so we use CamelCase
  - font-size => fontSize
  - background-color => backgroundColor

## Getter
- value = 

## Setter
- blah blah = value

## innerHTML
- we can set or get the value of the elements HTML
- allows us to manipulate the HTML, within the webpage

## Events
- we can listen for events and perform actions when they occur